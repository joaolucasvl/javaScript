function calcular(a, b) {
    const soma = a + b
    const sub = a - b
    const mult = a * b
    const div = a / b
    
    console.log(`Soma: ${soma}`)
    console.log(`Subtraçao: ${sub}`)
    console.log(`Multiplicação: ${mult}`)
    console.log(`Divisão: ${div}`)
}

calcular(10 , 4)

